# Shared defaults for all sync-stack environments. Per-env files in
# config/<env>/.env override these. Loaded by up.sh BEFORE the per-env
# file so the per-env file wins.

COUCHDB_USER=admin
TRICHO_META_DB=tricho_meta
PORT=4545
BILLING_ENABLED=true
TRAEFIK_LOG_LEVEL=INFO
