# Pinned image references. Normally identical to the deploying SHA (set
# by the workflow), but kept here so an operator can override one image's
# tag for emergency rollback without a git push.
#
# When IMAGE_TAG is set in the workflow environment, it takes precedence;
# the values below are last-resort defaults if up.sh is invoked manually.

# IMAGE_TAG=sha-...
