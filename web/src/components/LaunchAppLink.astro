---
// Hidden by default. Revealed by an inline script in Base.astro when the
// page is loaded inside the installed PWA (display-mode: standalone), so
// the user has a one-tap path back to the app from the marketing landing.
---

<a href="/app/" class="btn btn--primary" data-launch-app hidden>Otevřít aplikaci</a>
