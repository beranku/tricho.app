# build output
dist/
# generated types
.astro/
# Paraglide JS compiler output (regenerated via Vite plugin)
src/paraglide/
# Inlang plugin cache (regenerated on demand)
project.inlang/cache/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
#   .env                 — committed; non-sensitive defaults for `docker compose`
#   .env.local           — developer-local overrides (NOT committed)
#   .env.production      — legacy, kept ignored
.env.local
.env.production
infrastructure/.env

# Runtime-decrypted secrets (SOPS → tmpfs-ish staging for compose)
.secrets-runtime/

# Playwright run artifacts
playwright-report/
test-results/
test-results-ui/

# Coverage output (per-tier summaries — committed baseline is coverage-baseline.json)
/coverage/

# Infrastructure secrets
infrastructure/traefik/acme.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Auto Claude data directory
.auto-claude/
.auto-claude-security.json
.auto-claude-status
.claude_settings.json

# Traefik Let's Encrypt state
infrastructure/traefik/letsencrypt/
infrastructure/traefik/.env

# Tricho-auth generated dev JWT keys
infrastructure/couchdb/tricho-auth/node_modules/
tricho-impl-v1.tar.gz
